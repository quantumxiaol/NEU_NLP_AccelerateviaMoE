# controlNN 门控网络

## 作用

controlNN 是一个门控网络（Gate Network），用于训练专家路由器（Expert Router）。它的核心作用是：

1. **专家选择**：根据输入的隐层表示（词向量），智能选择应该激活哪些专家（Expert）
2. **加速推理**：通过只激活部分专家而不是全部专家，减少计算量，实现模型加速
3. **保持性能**：在减少计算量的同时，尽可能保持模型的翻译性能

## 原理

### 1. 网络结构

门控网络 `gateNet` 是一个浅层神经网络：
- **输入层**：256维的隐层表示（词向量）
- **隐藏层**：64维，使用ReLU激活函数
- **输出层**：8维（对应8个专家），输出每个专家的选择概率

网络结构：
```
输入(256) -> Linear(256->64) -> ReLU -> Linear(64->8) -> 输出(8)
```

### 2. 训练数据构造

训练数据通过以下方式构造：
1. 输入测试样例到原始Transformer模型
2. 记录每个位置的词向量（256维隐层表示）
3. 通过贪婪算法找到该位置应该激活的最佳专家组合
4. 将词向量作为输入，专家标签作为目标，构造训练数据集

### 3. 训练过程

- **损失函数**：交叉熵损失（CELoss）
- **优化器**：Adam，学习率1e-3，权重衰减1e-5
- **训练策略**：使用早停（patience=20）防止过拟合
- **数据划分**：80%训练集，20%验证集

### 4. 推理使用

在推理阶段：
1. 加载训练好的门控网络权重（`controlNN/weights/best.pth`）
2. 对于每个输入词向量，通过门控网络得到专家选择概率
3. 根据概率选择top-k个专家进行激活
4. 只计算被选中专家的输出，从而减少计算量

### 5. 与MoE模型的关系

在混合专家（MoE）模型中：
- **专家划分**：FFN层被划分为8个专家，每个专家包含部分神经元
- **门控网络**：controlNN决定每个位置应该激活哪些专家
- **稀疏激活**：只激活部分专家，而不是全部专家，实现稀疏计算
- **性能平衡**：在加速和精度之间找到平衡点

## 文件说明

- `model/gateModel.py`: 门控网络模型定义
- `model/myLoss.py`: 损失函数定义
- `utils/readData.py`: 数据加载器
- `train.py`: 训练脚本
- `vali.py`: 验证脚本
- `weights/`: 保存训练好的模型权重
